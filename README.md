# minetest-csm-compass
Adds half-transparent compass over crosshair
* Enable / disable it using `.compass` chatcommand
* Change mode using `.compass-mode <mode>`, where `<mode>` is `1`(radial) or `2`(horizontal)
